import utils.Day
import utils.IO

fun main() {
    Day23(IO.TYPE.SAMPLE).test()
    Day23().solve()
}

class Day23(inputType: IO.TYPE = IO.TYPE.INPUT) : Day("", inputType = inputType) {
    override fun part1(): Any? {
        return "not yet implement"
    }

    override fun part2(): Any? {
        return "not yet implement"
    }
}           